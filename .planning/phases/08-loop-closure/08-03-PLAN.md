---
phase: 08-loop-closure
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/feedback-inbox-read.js
  - scripts/feedback-to-memory.js
autonomous: true
requirements: [LOOP-03, LOOP-04]
must_haves:
  truths:
    - "feedback-inbox-read returns entries after the last cursor position, not already-processed ones"
    - "feedback-inbox-read advances cursor on read, resumes from correct position on next call"
    - "feedback-to-memory accepts JSON on stdin and emits MCP memory format on stdout"
    - "feedback-to-memory rejects bare signals (no context) with ok=false"
  artifacts:
    - path: "scripts/feedback-inbox-read.js"
      provides: "Cursor-based inbox reader — getNewEntries, readInbox, loadCursor, saveCursor"
      exports: [getNewEntries, readInbox, loadCursor, saveCursor, INBOX_PATH, CURSOR_PATH]
    - path: "scripts/feedback-to-memory.js"
      provides: "Feedback-to-MCP-memory bridge — convertFeedbackToMemory"
      exports: [convertFeedbackToMemory]
  key_links:
    - from: "scripts/feedback-inbox-read.js"
      to: ".claude/feedback-loop/inbox.jsonl"
      via: "INBOX_PATH = path.join(PROJECT_ROOT, '.claude', 'feedback-loop', 'inbox.jsonl')"
      pattern: "INBOX_PATH.*feedback-loop"
    - from: "scripts/feedback-to-memory.js"
      to: "scripts/feedback-schema.js"
      via: "require('./feedback-schema') — resolveFeedbackAction + prepareForStorage"
      pattern: "require.*feedback-schema"
---

<objective>
Port feedback-inbox-read.js and feedback-to-memory.js from Subway to rlhf-feedback-loop.

- feedback-inbox-read: Cursor-based JSONL inbox reader for reflexion-preflight. Reads new feedback entries from .claude/feedback-loop/inbox.jsonl using a cursor file to avoid re-reads. (LOOP-03)
- feedback-to-memory: stdin JSON → validated MCP memory format on stdout. The validation boundary between raw feedback signals and MCP memory storage. (LOOP-04)

Purpose: Complete the bidirectional feedback flow — inbox pulls in external signals, memory bridge converts them to storable format.
Output: scripts/feedback-inbox-read.js + scripts/feedback-to-memory.js
</objective>

<execution_context>
@/Users/ganapolsky_i/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ganapolsky_i/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Source references (Subway implementations to port):
@/Users/ganapolsky_i/workspace/git/Subway_RN_Demo/scripts/feedback-inbox-read.js
@/Users/ganapolsky_i/workspace/git/Subway_RN_Demo/scripts/feedback-to-memory.js

rlhf feedback-schema.js (already has resolveFeedbackAction + prepareForStorage):
@scripts/feedback-schema.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/feedback-inbox-read.js</name>
  <files>scripts/feedback-inbox-read.js</files>
  <action>
Port feedback-inbox-read.js from Subway's scripts/feedback-inbox-read.js to rlhf's scripts/feedback-inbox-read.js.

Path adaptations (Subway used PROJECT_ROOT = path.join(__dirname, '..'), same here — scripts/ is 1 level from repo root):
- PROJECT_ROOT = path.join(__dirname, '..')
- INBOX_PATH = path.join(PROJECT_ROOT, '.claude', 'feedback-loop', 'inbox.jsonl')
- CURSOR_PATH = path.join(PROJECT_ROOT, '.claude', 'feedback-loop', 'inbox.cursor.json')

Preserve exactly:
- readInbox(): reads INBOX_PATH, splits by '\n', JSON.parse each line with _lineIndex added, filters null
- loadCursor(): reads CURSOR_PATH, returns { lastLineIndex: -1 } if missing or invalid
- saveCursor(cursor): mkdirSync recursive, writes JSON to CURSOR_PATH
- getNewEntries(advance): filters entries where _lineIndex > cursor.lastLineIndex; if advance=true, saves cursor to max idx; strips _lineIndex before returning
- CLI modes: default (advance), --peek (no advance), --reset (delete cursor), --test (built-in tests)
- module.exports = { getNewEntries, readInbox, loadCursor, saveCursor, INBOX_PATH, CURSOR_PATH }
- require.main === module guard

The built-in --test mode from Subway uses monkey-patching of module paths which is incompatible with CJS exports. Port the tests as inline self-test (keep the same test assertions but use the actual getNewEntries/readInbox/loadCursor/saveCursor via tmpDir path overriding via env or direct calls with temp files). Keep --test flag working.

File must be 'use strict', CommonJS.
  </action>
  <verify>
    node -e "const m = require('./scripts/feedback-inbox-read.js'); console.log(Object.keys(m).join(','))"
    # Expected keys include: getNewEntries,readInbox,loadCursor,saveCursor,INBOX_PATH,CURSOR_PATH

    # Functional round-trip test:
    node -e "
    const os = require('os'), fs = require('fs'), path = require('path');
    const { readInbox, loadCursor, saveCursor, getNewEntries } = require('./scripts/feedback-inbox-read.js');
    // Override paths via env is not available, so test with direct function calls on temp files
    // readInbox reads INBOX_PATH — test that it returns [] gracefully when file missing
    // (INBOX_PATH won't exist in CI, so should return [])
    const result = readInbox();
    console.assert(Array.isArray(result), 'readInbox returns array');
    const cursor = loadCursor();
    console.assert(typeof cursor.lastLineIndex === 'number', 'loadCursor returns object with lastLineIndex');
    console.log('PASS inbox-read module tests');
    "
  </verify>
  <done>
    - scripts/feedback-inbox-read.js exists and exports all 6 items
    - readInbox() returns [] gracefully when inbox.jsonl does not exist
    - loadCursor() returns { lastLineIndex: -1 } when cursor file missing
    - getNewEntries(false) returns [] when no inbox (no crash)
    - CLI --peek mode exits without advancing cursor
    - CLI --reset removes cursor file if present
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/feedback-to-memory.js</name>
  <files>scripts/feedback-to-memory.js</files>
  <action>
Port feedback-to-memory.js from Subway's scripts/feedback-to-memory.js to rlhf's scripts/feedback-to-memory.js.

The main adaptation: Subway's feedback-to-memory.js uses require('./feedback-schema') which is at the same level (scripts/). In rlhf, scripts/feedback-schema.js is also at the same level, so the require path is identical.

Preserve exactly:
- convertFeedbackToMemory(params): calls resolveFeedbackAction({ signal, context, whatWentWrong, whatToChange, whatWorked, tags: params.tags || [] }); if action.type === 'no-action' → return { ok: false, reason: action.reason }; calls prepareForStorage(action.memory); if !prep.ok → return { ok: false, reason: 'Schema validation failed: ...', issues: prep.issues }; return { ok: true, actionType: action.type, memory: prep.memory }
- CLI runStdin(): reads stdin, JSON.parse, calls convertFeedbackToMemory, writes JSON result to stdout, exits 0 or 2
- CLI --test mode with built-in assertions (same test cases as Subway source)
- module.exports = { convertFeedbackToMemory }
- require.main === module guard

File must be 'use strict', CommonJS.
  </action>
  <verify>
    node -e "const m = require('./scripts/feedback-to-memory.js'); console.log(Object.keys(m).join(','))"
    # Expected: convertFeedbackToMemory

    node -e "
    const { convertFeedbackToMemory } = require('./scripts/feedback-to-memory.js');
    const neg = convertFeedbackToMemory({ signal: 'negative', context: 'Agent claimed fix without test evidence', whatWentWrong: 'No tests were run', whatToChange: 'Always run tests', tags: ['verification'] });
    console.assert(neg.ok === true, 'valid negative -> ok');
    console.assert(neg.actionType === 'store-mistake', 'negative -> store-mistake');
    console.assert(neg.memory.title.startsWith('MISTAKE:'), 'MISTAKE: prefix');
    const bare = convertFeedbackToMemory({ signal: 'negative' });
    console.assert(bare.ok === false, 'bare negative rejected');
    console.log('PASS feedback-to-memory tests');
    "
  </verify>
  <done>
    - scripts/feedback-to-memory.js exists and exports convertFeedbackToMemory
    - valid negative feedback returns { ok: true, actionType: 'store-mistake', memory: { title: 'MISTAKE:...', ... } }
    - valid positive feedback returns { ok: true, actionType: 'store-learning', memory: { title: 'SUCCESS:...', ... } }
    - bare negative (no context) returns { ok: false }
    - stdin round-trip works: echo '{"signal":"positive","whatWorked":"Ran tests","tags":["testing"]}' | node scripts/feedback-to-memory.js exits 0 with JSON on stdout
    - --test mode exits 0 with all assertions passing
  </done>
</task>

</tasks>

<verification>
node -e "require('./scripts/feedback-inbox-read.js')" && echo "inbox-read loads OK"
node -e "require('./scripts/feedback-to-memory.js')" && echo "feedback-to-memory loads OK"
echo '{"signal":"positive","whatWorked":"Test driven approach","tags":["testing"]}' | node scripts/feedback-to-memory.js
</verification>

<success_criteria>
- Both scripts present and loading without errors
- Cursor-based read logic functions correctly (new entries filtered by cursor position)
- Memory bridge converts valid feedback to MCP format and rejects bare signals
- LOOP-03 and LOOP-04 requirements satisfied
</success_criteria>

<output>
After completion, create .planning/phases/08-loop-closure/08-03-SUMMARY.md
</output>
