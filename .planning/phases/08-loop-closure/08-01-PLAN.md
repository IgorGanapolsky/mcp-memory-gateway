---
phase: 08-loop-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/feedback-to-rules.js
autonomous: true
requirements: [LOOP-01]
must_haves:
  truths:
    - "Running feedback-to-rules against a seeded feedback log produces at least one actionable rule"
    - "Rules output follows CLAUDE.md-compatible format (# header + severity-prefixed bullet lines)"
    - "Script handles missing log file gracefully (returns empty analysis, does not crash)"
  artifacts:
    - path: "scripts/feedback-to-rules.js"
      provides: "Feedback pattern distillation — parseFeedbackFile, classifySignal, analyze, toRules, normalize"
      exports: [parseFeedbackFile, classifySignal, analyze, toRules, normalize]
  key_links:
    - from: "scripts/feedback-to-rules.js"
      to: ".claude/memory/feedback/feedback-log.jsonl"
      via: "DEFAULT_LOG path.join(__dirname, '..', '.claude', 'memory', 'feedback', 'feedback-log.jsonl')"
      pattern: "parseFeedbackFile.*DEFAULT_LOG"
---

<objective>
Port feedback-to-rules.js from Subway to rlhf-feedback-loop.

This module reads the feedback JSONL log, analyzes negative signal patterns for recurring issues, and emits CLAUDE.md-compatible prevention rules. It closes the feedback → rules side of the RLHF loop (LOOP-01).

Purpose: Give the system a way to automatically distill repeated negative feedback into actionable behavior constraints without manual intervention.
Output: scripts/feedback-to-rules.js — fully functional, exported module + CLI
</objective>

<execution_context>
@/Users/ganapolsky_i/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ganapolsky_i/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Source reference (Subway implementation to port):
@/Users/ganapolsky_i/workspace/git/Subway_RN_Demo/.claude/scripts/feedback/feedback-to-rules.js

Existing rlhf scripts for path/pattern reference:
@scripts/feedback-loop.js
@scripts/feedback-attribution.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/feedback-to-rules.js</name>
  <files>scripts/feedback-to-rules.js</files>
  <action>
Port feedback-to-rules.js from Subway's .claude/scripts/feedback/feedback-to-rules.js to rlhf's scripts/ directory.

Key adaptations vs Subway source:
- DEFAULT_LOG path: use path.join(__dirname, '..', '.claude', 'memory', 'feedback', 'feedback-log.jsonl') — 1 level up from scripts/ (Subway used 2 levels from .claude/scripts/feedback/)
- All exported functions stay identical: parseFeedbackFile, classifySignal, analyze, toRules, normalize
- NEG set: ['negative', 'negative_strong', 'down', 'thumbs_down']
- POS set: ['positive', 'positive_strong', 'up', 'thumbs_up']
- analyze() returns: { generatedAt, totalFeedback, negativeCount, positiveCount, negativeRate, recurringIssues, categoryBreakdown, topTools }
- recurringIssues filters contextCounts where count >= 2, assigns severity: count>=4 'critical', count>=3 'high', else 'medium'
- toRules() emits markdown lines: "# Suggested Rules from Feedback Analysis" header, then "- [SEVERITY] (Nx) NEVER {description}" bullets
- CLI: node scripts/feedback-to-rules.js [log-path] [--rules] — --rules flag emits toRules() text, default emits JSON report
- require.main === module guard for CLI
- module.exports = { parseFeedbackFile, classifySignal, analyze, toRules, normalize }

File must be 'use strict', CommonJS (no ESM).
  </action>
  <verify>
    node -e "const m = require('./scripts/feedback-to-rules.js'); console.log(Object.keys(m).join(','))"
    # Expected: parseFeedbackFile,classifySignal,analyze,toRules,normalize

    # Smoke test with a temp JSONL:
    node -e "
    const os = require('os'), fs = require('fs'), path = require('path');
    const { parseFeedbackFile, analyze, toRules } = require('./scripts/feedback-to-rules.js');
    const tmp = path.join(os.tmpdir(), 'fb-test.jsonl');
    const entries = [
      {signal:'negative',context:'Agent claimed done without running tests',task_category:'testing',tool_name:'Bash'},
      {signal:'negative',context:'Agent claimed done without running tests',task_category:'testing',tool_name:'Bash'},
      {signal:'positive',context:'Ran full suite before responding',task_category:'testing'},
    ];
    fs.writeFileSync(tmp, entries.map(e=>JSON.stringify(e)).join('\n'));
    const parsed = parseFeedbackFile(tmp);
    console.assert(parsed.length === 3, 'parsed 3 entries');
    const report = analyze(parsed);
    console.assert(report.negativeCount === 2, 'negativeCount=2');
    console.assert(report.positiveCount === 1, 'positiveCount=1');
    console.assert(report.recurringIssues.length >= 1, 'at least 1 recurring issue');
    const rules = toRules(report);
    console.assert(rules.includes('NEVER'), 'rules contain NEVER keyword');
    console.log('PASS smoke test');
    fs.unlinkSync(tmp);
    "
  </verify>
  <done>
    - scripts/feedback-to-rules.js exists and exports { parseFeedbackFile, classifySignal, analyze, toRules, normalize }
    - analyze() returns recurringIssues for context strings appearing >= 2 times in negative feedback
    - toRules() produces CLAUDE.md-compatible markdown with NEVER-prefixed rule bullets
    - node scripts/feedback-to-rules.js exits 0 on missing log (no crash)
    - Smoke test passes with assert output "PASS smoke test"
  </done>
</task>

</tasks>

<verification>
node -e "require('./scripts/feedback-to-rules.js')" && echo "Module loads OK"
node scripts/feedback-to-rules.js && echo "CLI no-log exit OK"
</verification>

<success_criteria>
- scripts/feedback-to-rules.js present, parses JSONL, analyzes negative patterns, generates prevention rules
- Module exports all 5 functions
- CLI handles missing log file without crash
- LOOP-01 requirement satisfied: running against seeded log produces actionable rules
</success_criteria>

<output>
After completion, create .planning/phases/08-loop-closure/08-01-SUMMARY.md
</output>
