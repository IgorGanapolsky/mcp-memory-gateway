---
phase: 08-loop-closure
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/plan-gate.js
autonomous: true
requirements: [LOOP-02]
must_haves:
  truths:
    - "Plan gate rejects a PRD with fewer than 3 clarifying questions resolved (exit code 1)"
    - "Plan gate passes a structurally valid PRD with all gates satisfied (exit code 0)"
    - "Plan gate emits human-readable PASS/BLOCKED result plus per-gate status lines"
  artifacts:
    - path: "scripts/plan-gate.js"
      provides: "PRD markdown gate validation — validatePlan, formatReport, countTableRows, countContracts, countValidationScenarios, getStatus"
      exports: [validatePlan, formatReport, countTableRows, countContracts, countValidationScenarios, getStatus]
  key_links:
    - from: "scripts/plan-gate.js"
      to: "PRD markdown file"
      via: "fs.readFileSync(resolved, 'utf-8') then validatePlan(content)"
      pattern: "validatePlan.*content"
---

<objective>
Port plan-gate.js from Subway to rlhf-feedback-loop.

This script validates PRD/plan markdown files against structural gates before agent execution — ensuring clarifying questions are resolved, contracts are defined, and validation scenarios exist. It is the guard that prevents malformed plans from spawning agents (LOOP-02).

Purpose: Gate enforcement before execution. Prevents underdefined plans from being executed by agents.
Output: scripts/plan-gate.js — fully functional, exported module + CLI
</objective>

<execution_context>
@/Users/ganapolsky_i/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ganapolsky_i/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Source reference (Subway implementation to port):
@/Users/ganapolsky_i/workspace/git/Subway_RN_Demo/scripts/plan-gate.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/plan-gate.js</name>
  <files>scripts/plan-gate.js</files>
  <action>
Port plan-gate.js directly from Subway's scripts/plan-gate.js to rlhf's scripts/plan-gate.js.

The source is identical in both repos — no path adaptations needed (reads a file passed as argv, not a hardcoded path).

Preserve exactly:
- countTableRows(content, sectionHeading): regex-based, subtracts header+separator rows, returns Math.max(0, dataRows.length - 1)
- countContracts(content): counts interface/type keywords inside code blocks under "## Contracts" section
- countValidationScenarios(content): counts "- [ ]" lines under "## Validation Checklist" section
- getStatus(content): extracts status after "## Status" heading
- validatePlan(content): gates = [Clarifying Questions>=3, Contracts>=1, Validation Checklist>=2, Status!='COMPLETE']
- formatReport(result): emits "✅/❌ Gate: detail" lines + "RESULT: PASS" or "RESULT: BLOCKED" line
- run(): CLI entry point, reads file from argv, supports --json flag
- module.exports = { validatePlan, formatReport, countTableRows, countContracts, countValidationScenarios, getStatus }
- require.main === module guard

File must be 'use strict', CommonJS (no ESM).
  </action>
  <verify>
    node -e "const m = require('./scripts/plan-gate.js'); console.log(Object.keys(m).join(','))"
    # Expected: validatePlan,formatReport,countTableRows,countContracts,countValidationScenarios,getStatus

    # Test with a failing PRD (missing questions):
    node -e "
    const { validatePlan, formatReport } = require('./scripts/plan-gate.js');
    const badPRD = '# My Plan\n\n## Status\nDRAFT\n\n## Contracts\n\`\`\`\ninterface Foo {}\n\`\`\`\n\n## Validation Checklist\n- [ ] test 1\n- [ ] test 2\n';
    const result = validatePlan(badPRD);
    console.assert(!result.allPass, 'bad PRD fails (missing questions)');
    const goodPRD = '# My Plan\n\n## Status\nDRAFT\n\n## Clarifying Questions Resolved\n| Q | A |\n|---|---|\n| q1 | a1 |\n| q2 | a2 |\n| q3 | a3 |\n\n## Contracts\n\`\`\`\ninterface Bar {}\n\`\`\`\n\n## Validation Checklist\n- [ ] test 1\n- [ ] test 2\n';
    const goodResult = validatePlan(goodPRD);
    console.assert(goodResult.allPass, 'good PRD passes all gates');
    console.log('PASS gate tests');
    "
  </verify>
  <done>
    - scripts/plan-gate.js exists and exports all 6 functions
    - validatePlan() returns allPass=false for PRD with fewer than 3 clarifying questions
    - validatePlan() returns allPass=true for structurally complete PRD
    - CLI exits 0 for valid PRD, exits 1 for invalid PRD
    - --json flag emits JSON result object
    - "PASS gate tests" printed from inline verify
  </done>
</task>

</tasks>

<verification>
node -e "require('./scripts/plan-gate.js')" && echo "Module loads OK"
node scripts/plan-gate.js 2>&1 | grep "Usage:" && echo "CLI usage message OK"
</verification>

<success_criteria>
- scripts/plan-gate.js present with all 6 exported functions
- Gate rejects PRD missing required sections (exit 1)
- Gate passes structurally valid PRD (exit 0)
- LOOP-02 requirement satisfied
</success_criteria>

<output>
After completion, create .planning/phases/08-loop-closure/08-02-SUMMARY.md
</output>
