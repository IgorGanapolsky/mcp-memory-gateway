---
phase: 08-loop-closure
plan: "04"
type: execute
wave: 2
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - tests/loop-closure.test.js
  - scripts/prove-loop-closure.js
  - proof/loop-closure-report.json
  - proof/loop-closure-report.md
  - package.json
autonomous: true
requirements: [LOOP-05]
must_haves:
  truths:
    - "npm test passes with 0 failures after adding loop-closure tests"
    - "test count increases beyond 184 (previous baseline)"
    - "prove-loop-closure.js exits 0 with all 5 LOOP requirements passing"
    - "proof/loop-closure-report.json exists with summary.passed=5, summary.failed=0"
  artifacts:
    - path: "tests/loop-closure.test.js"
      provides: "Unit tests for all 4 loop-closure modules"
    - path: "scripts/prove-loop-closure.js"
      provides: "Proof gate — verifies LOOP-01 through LOOP-05"
    - path: "proof/loop-closure-report.json"
      provides: "Machine-readable proof result"
    - path: "proof/loop-closure-report.md"
      provides: "Human-readable proof report"
  key_links:
    - from: "tests/loop-closure.test.js"
      to: "scripts/feedback-to-rules.js + scripts/plan-gate.js + scripts/feedback-inbox-read.js + scripts/feedback-to-memory.js"
      via: "require() each module, test each exported function"
      pattern: "require.*loop-closure|feedback-to-rules|plan-gate|feedback-inbox|feedback-to-memory"
---

<objective>
Write test suite + proof gate for all Phase 8 loop-closure modules.

Tests cover: feedback-to-rules analysis, plan-gate validation, inbox reader cursor logic, feedback-to-memory bridge. Proof gate generates machine-readable LOOP-01..LOOP-05 pass/fail evidence.

Purpose: Close LOOP-05 (unit tests for all loop-closure features) and produce proof artifacts for Phase 12.
Output: tests/loop-closure.test.js + scripts/prove-loop-closure.js + proof/ report files + package.json wiring
</objective>

<execution_context>
@/Users/ganapolsky_i/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ganapolsky_i/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Existing proof gate pattern to mirror:
@scripts/prove-attribution.js
@scripts/prove-rlaif.js

Loop-closure modules under test:
@scripts/feedback-to-rules.js
@scripts/plan-gate.js
@scripts/feedback-inbox-read.js
@scripts/feedback-to-memory.js

Existing test pattern:
@tests/feedback-attribution.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/loop-closure.test.js using node:test</name>
  <files>tests/loop-closure.test.js</files>
  <action>
Write tests/loop-closure.test.js using node:test (describe/it pattern) with require.cache invalidation for module isolation.

Test structure — 4 describe blocks, one per module:

**describe('feedback-to-rules'):**
- it('parseFeedbackFile returns [] for missing file')
- it('parseFeedbackFile reads and parses entries from JSONL')
- it('classifySignal: negative maps to negative, positive maps to positive, unknown returns null')
- it('normalize strips /Users paths and port numbers, lowercases')
- it('analyze counts positive/negative/categories correctly')
- it('analyze produces recurringIssues for context appearing >= 2 times with correct severity')
- it('toRules emits CLAUDE.md-compatible markdown with NEVER bullets')
- it('toRules emits empty-issues message when no recurring issues')

**describe('plan-gate'):**
- it('countTableRows returns 0 for missing section')
- it('countTableRows counts data rows excluding header and separator')
- it('countContracts counts interface/type keywords in Contracts section code blocks')
- it('countValidationScenarios counts unchecked [ ] items in Validation Checklist')
- it('validatePlan fails when Clarifying Questions < 3')
- it('validatePlan fails when Contracts = 0')
- it('validatePlan fails when Validation Checklist < 2')
- it('validatePlan fails when Status = COMPLETE')
- it('validatePlan passes when all gates satisfied and status is not COMPLETE')
- it('formatReport emits RESULT: PASS for allPass=true')
- it('formatReport emits RESULT: BLOCKED for allPass=false')

**describe('feedback-inbox-read'):**
- it('readInbox returns [] when inbox file does not exist')
- it('loadCursor returns { lastLineIndex: -1 } when cursor file missing')
- it('getNewEntries returns [] when inbox is empty')
- it('getNewEntries filters by cursor position — only returns entries after lastLineIndex')
- it('getNewEntries advances cursor when advance=true')
- it('getNewEntries does not advance cursor when advance=false (peek)')
- it('strips _lineIndex from returned entries')

Use os.tmpdir() + fs.mkdtempSync for inbox-read tests that need real files. Override INBOX_PATH / CURSOR_PATH by directly calling readInbox/loadCursor with temp paths — since these are internal to the module and not env-injectable, write helper functions that call the module's internal logic via parameter-based wrappers (or test via the exported functions using temp files written to the actual INBOX_PATH temporarily — use beforeEach/afterEach cleanup). Best approach: create a tmp inbox.jsonl, call readInbox() which reads INBOX_PATH, so temporarily write to INBOX_PATH location — but that's risky. Instead: test getNewEntries indirectly by calling it with a seeded inbox at the actual path (tmp env with RLHF_FEEDBACK_DIR-style override) OR just test that getNewEntries() returns [] gracefully when no file, and test the cursor filtering logic directly by calling loadCursor/saveCursor with tmpdir paths.

Simplest reliable approach: for cursor tests, test loadCursor/saveCursor directly with tmpdir paths (they accept path params — actually they don't, they use constants). So: use a subdirectory of os.tmpdir() and write inbox.jsonl there, then require.cache-invalidate and set INBOX_PATH-equivalent via a re-require with process.env approach if available. Since INBOX_PATH is a module constant and not env-injectable in this port, test what we can: empty-file behavior, and use readInbox directly by temporarily writing to the actual INBOX_PATH location in a mkdtempSync and testing the full flow with process.chdir or by mocking fs — avoid mocking.

Practical decision: Test readInbox(), loadCursor(), saveCursor() directly by writing real temp files to INBOX_PATH after reading its value from the export, swapping the file, then restoring — or simply cover the structural logic via the analyze/classifySignal functions which don't need files. For file-dependent functions, use a before/after approach writing to a known temp location and validating behavior. Keep tests focused and passing.

**describe('feedback-to-memory'):**
- it('convertFeedbackToMemory: valid negative returns ok=true, store-mistake, MISTAKE: prefix')
- it('convertFeedbackToMemory: valid positive returns ok=true, store-learning, SUCCESS: prefix')
- it('convertFeedbackToMemory: bare negative (no context) returns ok=false')
- it('convertFeedbackToMemory: bare positive (no context) returns ok=false')
- it('convertFeedbackToMemory: unknown signal returns ok=false')
- it('convertFeedbackToMemory: context-only negative returns ok=true')
- it('convertFeedbackToMemory: preserves domain tags in output memory')

Use node:test import: const { describe, it } = require('node:test'); const assert = require('node:assert/strict');
Target: at least 25 new tests total in this file.
  </action>
  <verify>
    node --test tests/loop-closure.test.js 2>&1 | tail -5
    # Must show: pass >= 25, fail = 0
  </verify>
  <done>
    - tests/loop-closure.test.js exists with 4 describe blocks covering all 4 modules
    - node --test tests/loop-closure.test.js passes with 0 failures
    - At least 25 test cases total
  </done>
</task>

<task type="auto">
  <name>Task 2: Write prove-loop-closure.js + generate proof reports + wire package.json</name>
  <files>scripts/prove-loop-closure.js, proof/loop-closure-report.json, proof/loop-closure-report.md, package.json</files>
  <action>
Write scripts/prove-loop-closure.js mirroring prove-attribution.js structure exactly (mkdtempSync / env override / execSync pattern).

**LOOP-01 check:** Create a tmp JSONL with 3+ negative entries sharing the same context string (at least 2). Call parseFeedbackFile + analyze. Assert recurringIssues.length >= 1 and the result includes at least one rule when toRules() is called. Assert rules string contains 'NEVER'.

**LOOP-02 check:** Build a valid PRD markdown string with all gates satisfied. Call validatePlan(content). Assert allPass=true. Build an invalid PRD (missing Clarifying Questions section). Call validatePlan(invalid). Assert allPass=false.

**LOOP-03 check:** Write a temp inbox.jsonl with 3 entries to a tmpdir. Since INBOX_PATH is hardcoded in the module, test the cursor filtering logic by directly calling: const entries = []; and simulate cursor filtering using the exported functions: write 3 entries to INBOX_PATH tmpdir, saveCursor({lastLineIndex: 0}), call the getNewEntries logic by temporarily requiring a fresh copy. Use require.cache delete trick + process.env to override INBOX_PATH if possible — or test directly using readInbox/loadCursor/saveCursor: write real files to tmpdir, call loadCursor (will return -1), call saveCursor({lastLineIndex:1}), verify cursor saved. Since INBOX_PATH constant is exported, use it to seed real file, call getNewEntries(false), assert array.

If INBOX_PATH is a path under .claude/feedback-loop/inbox.jsonl (which may not exist), getNewEntries returns []. Write the actual inbox.jsonl temporarily: mkdirSync(path.dirname(INBOX_PATH), {recursive:true}), write 3 entries, call getNewEntries(false), assert length=3, cleanup.

**LOOP-04 check:** Import convertFeedbackToMemory. Call with valid negative params. Assert ok=true and memory.title starts with 'MISTAKE:'. Call with bare signal. Assert ok=false.

**LOOP-05 check:** Use execSync to run: node --test tests/loop-closure.test.js. Parse output for pass/fail. Assert fail=0 and pass >= 1.

Write proof/loop-closure-report.json: { "summary": { "passed": N, "failed": 0 }, "requirements": [ {id, status, evidence} x5 ] }
Write proof/loop-closure-report.md: Phase 8 loop closure proof table with per-requirement PASS/FAIL rows.

**package.json wiring:**
1. Add test:loop-closure to test:api's node --test invocation (append tests/loop-closure.test.js)
2. Add "prove:loop-closure": "node scripts/prove-loop-closure.js" script

Run prove-loop-closure.js at the end of this task to generate the actual proof files with real test output.
  </action>
  <verify>
    node scripts/prove-loop-closure.js
    # Must exit 0 with "passed: 5  failed: 0" output

    cat proof/loop-closure-report.json | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.assert(d.summary.passed===5,'5 passed'); console.assert(d.summary.failed===0,'0 failed'); console.log('PASS proof JSON')"

    node --test tests/loop-closure.test.js 2>&1 | grep -E "^ℹ" | tail -3
  </verify>
  <done>
    - scripts/prove-loop-closure.js exists and exits 0
    - proof/loop-closure-report.json has summary.passed=5, summary.failed=0
    - proof/loop-closure-report.md exists with per-requirement evidence
    - package.json test:api includes tests/loop-closure.test.js
    - package.json has prove:loop-closure script
    - npm test passes with count > 184
  </done>
</task>

</tasks>

<verification>
npm test 2>&1 | grep -E "pass|fail" | tail -10
node scripts/prove-loop-closure.js
cat proof/loop-closure-report.json
</verification>

<success_criteria>
- tests/loop-closure.test.js has >= 25 tests, all passing
- prove-loop-closure.js exits 0 with all 5 LOOP requirements satisfied
- proof/ report files exist with real (non-placeholder) evidence
- npm test count increases beyond 184 baseline
- LOOP-05 and all LOOP-01..LOOP-04 requirements satisfied
</success_criteria>

<output>
After completion, create .planning/phases/08-loop-closure/08-04-SUMMARY.md
</output>
