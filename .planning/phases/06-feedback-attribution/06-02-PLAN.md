---
phase: 06-feedback-attribution
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/hybrid-feedback-context.js
autonomous: true
requirements:
  - ATTR-02

must_haves:
  truths:
    - "scripts/hybrid-feedback-context.js exists and exports buildHybridState, evaluatePretool, compileGuardArtifact, writeGuardArtifact, readGuardArtifact, evaluateCompiledGuards, evaluatePretoolFromState"
    - "evaluatePretool('Bash', 'git push --force') returns mode: 'block' or 'warn' when attributed negative patterns exist for that tool"
    - "evaluatePretool('Read', 'some new file') returns mode: 'allow' when no attributed negative patterns match"
    - "compileGuardArtifact() + writeGuardArtifact() produces a valid pretool-guards.json at .claude/memory/feedback/pretool-guards.json"
    - "CLI --pretool exits 2 on block, 0 on allow/warn; --compile-guards writes guard artifact and outputs JSON summary"
    - "All paths resolve correctly from scripts/ (ROOT one level up, data at .claude/memory/feedback/)"
  artifacts:
    - path: "scripts/hybrid-feedback-context.js"
      provides: "pre-tool guard engine — buildHybridState, evaluatePretool, compileGuardArtifact, guard artifact I/O"
      min_lines: 400
      exports:
        - buildHybridState
        - evaluatePretool
        - compileGuardArtifact
        - writeGuardArtifact
        - readGuardArtifact
        - evaluateCompiledGuards
        - evaluatePretoolFromState
        - deriveConstraints
        - buildAdditionalContext
  key_links:
    - from: "scripts/hybrid-feedback-context.js buildHybridState()"
      to: ".claude/memory/feedback/attributed-feedback.jsonl"
      via: "PATHS.attributedFeedback — populated by feedback-attribution.js attributeFeedback()"
      pattern: "attributedFeedback"
    - from: "scripts/hybrid-feedback-context.js evaluatePretool()"
      to: "scripts/hybrid-feedback-context.js readGuardArtifact() + evaluateCompiledGuards()"
      via: "compiled guard artifact takes precedence over live state — same two-phase lookup pattern as Subway"
      pattern: "readGuardArtifact"
    - from: "scripts/hybrid-feedback-context.js compileGuardArtifact()"
      to: "state.recurringNegativePatterns"
      via: "attributedPatterns filter: prefers patterns sourced from attributedFeedback source over raw negatives"
      pattern: "attributedFeedback"
---

<objective>
Port hybrid-feedback-context.js from Subway to rlhf as scripts/hybrid-feedback-context.js — the pre-tool execution guard that blocks or warns based on attributed feedback history.

Purpose: ATTR-02 requires pre-tool guard calls to return block/warn/allow decisions derived from attributed feedback, not hard-coded rules. This module consumes the attributed-feedback.jsonl produced by the attribution engine (Plan 01) and compiles it into a fast guard artifact.

Output: scripts/hybrid-feedback-context.js (new module, fully standalone).
</objective>

<execution_context>
@/Users/ganapolsky_i/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ganapolsky_i/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/rlaif-self-audit.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port hybrid-feedback-context.js to scripts/hybrid-feedback-context.js with rlhf path corrections</name>
  <files>scripts/hybrid-feedback-context.js</files>
  <action>
Create `scripts/hybrid-feedback-context.js` by porting Subway's `.claude/scripts/feedback/hybrid-feedback-context.js` with these exact adjustments:

**Path correction (CRITICAL):**
- Subway ROOT = `path.join(__dirname, '..', '..')` (from `.claude/scripts/feedback/`)
- rlhf ROOT = `path.join(__dirname, '..')` (from `scripts/`)
- rlhf PATHS must be:
  ```javascript
  const PATHS = {
    feedbackLog: path.join(ROOT, '.claude', 'memory', 'feedback', 'feedback-log.jsonl'),
    inbox: path.join(ROOT, '.claude', 'memory', 'feedback', 'inbox.jsonl'),
    pendingSync: path.join(ROOT, '.claude', 'memory', 'feedback', 'pending_cortex_sync.jsonl'),
    attributedFeedback: path.join(ROOT, '.claude', 'memory', 'feedback', 'attributed-feedback.jsonl'),
    guardArtifact: path.join(ROOT, '.claude', 'memory', 'feedback', 'pretool-guards.json'),
  };
  ```

**Environment variable names (keep identical to Subway):**
- `process.env.RLHF_FEEDBACK_LOG`
- `process.env.RLHF_FEEDBACK_INBOX`
- `process.env.RLHF_PENDING_SYNC`
- `process.env.RLHF_ATTRIBUTED_FEEDBACK`
- `process.env.RLHF_GUARDS_PATH`

**Everything else ports verbatim from Subway source:**
- STOPWORDS set — includes 'bash', 'edit', 'write', 'tool', 'hook', 'clear' (note: slightly different from feedback-attribution.js STOPWORDS — keep this file's version)
- NEG / POS sets for signal classification
- `readJsonl(filePath, maxLines = 400)` — same reverse-read pattern
- `normalize(text)` — identical to attribution version
- `stripFeedbackPrefix(text)` — identical
- `normalizePatternText(text)` — compose normalize + stripFeedbackPrefix
- `inferToolName(rawToolName, context)` — infers tool from context keywords when explicit name is unknown
- `classify(entry)` — maps signal/feedback field to 'positive'/'negative'/'neutral'
- `getTimestampMs(value)` — Date.parse with 0 fallback
- `keywords(text)` — min 4 chars, no stopwords, max 8 (different from attribution tokenize: min 3, max 24)
- `hashText(text)` — FNV-1a (identical implementation)
- `buildHybridState(opts)` — reads all 4 JSONL sources, deduplicates, builds recurringNegativePatterns (count >= 2), preventionRules, negativeToolCounts, negativeToolCountsAttributed
- `deriveConstraints(state, max)` — produces up to 5 actionable constraint strings from patterns + prevention rules
- `buildAdditionalContext(state, constraints, maxChars)` — formats a single summary string for pre-tool context injection
- `hasTwoKeywordHits(normalizedInput, words)` — requires 2+ keyword matches to reduce false positives
- `compileGuardArtifact(state, opts)` — builds deduped guards array, prefers attributed patterns, assigns block/warn mode
- `writeGuardArtifact(filePath, artifact)` — atomic write via tmp rename (${outPath}.tmp.${process.pid}.${Date.now()})
- `readGuardArtifact(filePath)` — reads + validates guards array exists
- `evaluateCompiledGuards(artifact, toolName, toolInput)` — fast path: checks compiled artifact first
- `evaluatePretoolFromState(state, toolName, toolInput)` — live path: checks recurringNegativePatterns + negativeToolCounts
- `evaluatePretool(toolName, toolInput, opts)` — orchestrates: compiled → state fallback
- `main()` — CLI: --pretool <toolName> <toolInputJson>, --compile-guards [outPath]; default prints full state+constraints+context

**Important behavioral invariant (must preserve):**
- `evaluatePretool` with a tool+input that has NEVER produced a negative returns `{mode: 'allow', reason: '', source: 'compiled'|'state'}`
- This is ATTR-03's "no false positives" requirement — `hasTwoKeywordHits` and the `count >= 2` filter enforce this

File header: `'use strict';` as first line. CJS only.
  </action>
  <verify>
node -e "const m = require('./scripts/hybrid-feedback-context'); console.log(Object.keys(m).join(','))" 2>&1
node -e "const m = require('./scripts/hybrid-feedback-context'); const d = m.evaluatePretool('Read', 'some-never-seen-file.md'); console.log('mode:', d.mode); process.exit(d.mode === 'allow' ? 0 : 1);" 2>&1
node scripts/hybrid-feedback-context.js 2>&1 | head -5
  </verify>
  <done>require('./scripts/hybrid-feedback-context') exports all 9 functions; evaluatePretool with no prior negative data returns {mode:'allow'}; CLI runs without error; no production JSONL files modified during smoke test</done>
</task>

</tasks>

<verification>
1. node -e "require('./scripts/hybrid-feedback-context')" — no error
2. node -e "const m=require('./scripts/hybrid-feedback-context'); const r=m.evaluatePretool('Read','never-seen'); console.log(r.mode === 'allow')"
3. node -e "const m=require('./scripts/hybrid-feedback-context'); const s=m.buildHybridState(); console.log(typeof s.counts.total === 'number')"
4. grep "attributedFeedback" scripts/hybrid-feedback-context.js — confirms PATHS key and preferred strategy
5. node scripts/hybrid-feedback-context.js --compile-guards /tmp/test-guards.json 2>&1 | head -3
</verification>

<success_criteria>
- scripts/hybrid-feedback-context.js created; all 9 functions exported
- evaluatePretool with no matching pattern returns mode: 'allow' (no false positives)
- PATHS.guardArtifact resolves to .claude/memory/feedback/pretool-guards.json
- CLI --compile-guards writes artifact and outputs JSON with guardCount
- CLI --pretool exits 2 for block, 0 for allow/warn
- No other scripts modified by this plan
</success_criteria>

<output>
After completion, create `.planning/phases/06-feedback-attribution/06-02-SUMMARY.md`
</output>
